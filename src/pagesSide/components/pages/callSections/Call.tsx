import scss from './Call.module.scss';

const Call = () => {
	return (
		<section className={scss.Call}>
			<div className="container">
				<div className={scss.content}>Call</div>
			</div>
		</section>
	);
};

export default Call;
